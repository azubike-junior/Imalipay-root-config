apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: root_config
  name: root_config
spec:
  replicas: 1
  selector:
    matchLabels:
      app: root_config
  strategy: {}
  template:
    metadata:
      labels:
        app: root_config
    spec:
      containers:
        - image: 386821460247.dkr.ecr.eu-west-1.amazonaws.com/root_config:latest
          name: root_config
          imagePullPolicy: Always
          resources: {}
          ports:
            - containerPort: 80
status: {}

---
apiVersion: v1
kind: Service
metadata:
  name: root_config
  labels:
    run: root_config
spec:
  ports:
    - port: 80
      protocol: TCP
  selector:
    app: root_config
  type: LoadBalancer

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: root_config
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx-example
  rules:
    - host: "development.imalipay.com"
      http:
        paths:
          - path: /root_config
            pathType: Prefix
            backend:
              service:
                name: root_config
                port:
                  number: 80
