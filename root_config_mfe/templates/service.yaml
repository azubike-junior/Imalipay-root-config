apiVersion: v1
kind: Service
metadata:
  name: {{ include "root_config_mfe.fullname" . }}
  labels:
    {{- include "root_config_mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "root_config_mfe.selectorLabels" . | nindent 4 }}
