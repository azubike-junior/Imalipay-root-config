apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "root_config_mfe.fullname" . }}-test-connection"
  labels:
    {{- include "root_config_mfe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "root_config_mfe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
