apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "root-config-mfe.fullname" . }}-test-connection"
  labels:
    {{- include "root-config-mfe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "root-config-mfe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
