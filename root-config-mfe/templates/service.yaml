apiVersion: v1
kind: Service
metadata:
  name: {{ include "root-config-mfe.fullname" . }}
  labels:
    {{- include "root-config-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "root-config-mfe.selectorLabels" . | nindent 4 }}
